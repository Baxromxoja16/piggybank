import { UpDirective } from './up.directive';

describe('UpDirective', () => {
  it('should create an instance', () => {
    const directive = new UpDirective();
    expect(directive).toBeTruthy();
  });
});
